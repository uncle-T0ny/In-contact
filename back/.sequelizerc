const fs = require('fs');
const path = require('path');

module.exports = {
  'config': path.resolve('./config/db-migrate.config.js'),
  'migrations-path': path.resolve('./migrations'),
  'models-path': path.resolve('./db/models')
};
